// comma second
            // else if (numberHold.size() > 3 
            // && ((j) == 1 || j == 4) 
            // && j != numberHold.size() - 1)
            // {
            //     commaNum += numberHold[j];
            //     commaNum += ',';
            // }
            // // comma third
            // else if (numberHold.size() > 3 
            // && ((j) == 2 || j == 5) 
            // && j != numberHold.size() - 1)
            // {
            //     commaNum += numberHold[j];
            //     commaNum += ',';
            // }